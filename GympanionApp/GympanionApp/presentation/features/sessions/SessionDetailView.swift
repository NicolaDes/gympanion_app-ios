// GympanionApp/presentation/features/sessions/SessionDetailView.swift
import SwiftUI

struct SessionDetailView: View {
    let sessionId: String

    var body: some View {
        Text("TODO: Session detail for \(sessionId)")
            .navigationTitle("Session")
    }
}
