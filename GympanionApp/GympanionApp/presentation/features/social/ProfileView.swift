// GympanionApp/presentation/features/social/ProfileView.swift
import SwiftUI

struct ProfileView: View {
    let userId: String?

    var body: some View {
        Text("TODO: Profile for user \(userId ?? "me")")
            .navigationTitle("Profile")
    }
}
